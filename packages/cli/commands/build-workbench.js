// `rig build-workbench` — regenerate paper-rig-workbench.html from the packages.
// Implemented in the workbench-generation milestone.

export function runBuildWorkbench() {
  console.error('build-workbench: not yet wired (workbench-generation milestone)');
  return 2;
}
